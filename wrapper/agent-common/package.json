{
  "name": "@kaoiro/agent-common",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kaoiro AI エージェント共通層 — 状態機械 / EngineAdapter interface / permission・question broker / 共通 Tool 記述層 (ADR-0017/0032 F1)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@kaoiro/wrapper-core": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@kaoiro/protocol": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
