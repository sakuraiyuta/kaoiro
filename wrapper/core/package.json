{
  "name": "@kaoiro/wrapper-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kaoiro ラッパー共通基盤 — transport / config / CLI 引数 (エンティティ非依存、ADR-0017/0032 F1)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node ../../scripts/generate-wrapper-build-info.mjs dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phoenix": "^1.8.7"
  },
  "devDependencies": {
    "@kaoiro/protocol": "workspace:*",
    "@types/node": "^22.10.0",
    "@types/phoenix": "^1.6.7",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
