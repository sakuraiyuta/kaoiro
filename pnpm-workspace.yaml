packages:
  - protocol
  - wrapper/core
  - wrapper/agent-common
  - wrapper/claude-code
  - wrapper/codex
  - wrapper/antigravity
  - runner
  # dashboard(トップレベル dashboard/)は別ビルド系のため非メンバ。
  # wrapper/ 直下はメタ shim (workspace 非メンバ、scripts のみ)。

# pnpm 10 build-script approval。vitest/tsx が引く esbuild の postinstall を許可。
onlyBuiltDependencies:
  - esbuild

patchedDependencies:
  '@openai/codex-sdk@0.144.1': patches/@openai__codex-sdk@0.144.1.patch
