# Elixir / Mix
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Phoenix
/priv/static/assets/
/priv/static/cache_manifest.json
/assets/node_modules/
npm-debug.log

# Persona pack ingest — content-hashed extraction cache is derived from
# the checked-in zips (ADR-0029, KaoiroServer.PersonaAssets).
server/priv/persona-packs/.cache/

# Node / pnpm (workspace root). Per-package .gitignore covers dist/ etc.
node_modules/
*.tsbuildinfo

# Runner distribution archives, plus the staging dirs and lock dir the build
# script creates (scripts/build-runner-tarball.sh, issue #70).
/dist-tarball/
/.tarball-build*

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
*~

# Generated persona sprites (reproducible via seeds in docs/specs/personas.md)
/assets-work/

agent.*.json

# Local dev scratch (scripts/dev.sh writes server/dashboard/runner logs here)
/tmp/
