---
title: Documentation deduplication plan for issue 372
status: done
last_updated: 2026-09-26
description: Canonical-page and pointer map for the five in-scope documentation deduplication units.
---

# Issue 372 documentation deduplication plan

Base: `origin/develop` at `263feaa7503ff315a946a8eec8c8778efb156fcc`. Scope excludes the Production cheat-sheet because the active issue 407 branch edits `docs/operations/production.md` (four added lines); it will be handled after that branch lands.

| Unit | Keep as canonical | Replace with pointer | Unique clauses that must survive at the canonical destination |
|---|---|---|---|
| Antigravity (U26) | `architecture/antigravity-adapter.md` for CLI-vs-SDK rationale and persona-prompt architecture; `reference/engines/antigravity-events.md` for stream/event/session contracts; `reference/engines/antigravity-tools-permissions.md` for tool/permission/bridge contracts; `evidence/antigravity/cli-contract.md` for measured customization, workspace-root, and MCP observations | Any cross-layer restatement among these four pages becomes a short link to its semantic owner; retain each page's unique heading as an anchor | Preserve these source headings and their clauses: architecture — `Why the CLI and not the Python SDK`, `System-prompt equivalent (persona personality injection)`; events — `Main API and process model`, `stream-json events (actual 1.1.26 shapes)`, `State derivation`, `Session / conversation resume and enumeration`, `Models, effort, usage, rate limits`, `Authentication and host requirements`, `Constraints`; tools — `Permission (hooks are the approval channel)`, `Tool children: prompts disabled, absolute tool deadline (issue #350)`, `Tool definition (CLI bridge over the wrapper tool host)`, `Constraints`; evidence — `Customization discovery (persona, hooks, skills)`, `MCP is not available in headless mode`. Keep CLI rationale and prompt refresh/display-name behavior in architecture; stream/state/session/model/auth contract in events; headless approval, tool-child deadline, and bridge contract in tools; measured `--add-dir` customization/root behavior and absent headless MCP in evidence. |
| Codex backend (U33) | `reference/configuration/runner.md#codex-backend` | Replace the duplicate `### Codex backend selection` subsection with a pointer to `## Codex backend` | Preserve in the canonical section: `codex_backend` wrapper relay and startup log; steering-disabled / approval-never state; reload affects only new wrapper lifetimes, including resume, reset, and automatic restart (existing children remain unchanged); reload log is not an applied notice; invalid startup/reload handling; rollback on same host/cwd; older-wrapper backward-compatibility requirement; no automatic exec fallback. |
| Install profile (U33) | `architecture/deployment.md#build-and-restart-boundaries` in-place-build row | Replace the general release-profile risk paragraph in `operations/runner-install.md` with a pointer to the architecture row | Preserve in the canonical architecture row the active-checkout mixed-artifact risk and its release-profile counterpart: builds and extraction stay isolated under `releases/<rev>/` and never touch a running release. Preserve the unique Codex lazy resolution condition (resolved on first spawn). Keep the source-origin/activation-layout distinction and checkout/local-build/Gitea deployment-forms table in the operations guide; they are outside the paragraph being deduplicated and will not be copied into architecture. |
| Tarball procedure (U19/U33) | `operations/runner-install.md#creating-distribution-tarballs` and its target-install subsection | Replace the duplicated introductory generation/install command block under `## 2. Deploy runners` with a pointer to the detailed canonical procedure | Preserve the Node-only target prerequisite, architecture-specific build commands, supported targets/output behavior, per-host install then switch order, and first-install versus `current/deploy` invocation location in the detailed section. |
| Runner update cross-references (U21) | `operations/server-update-and-rollback.md#43-update-procedure` and `#44-failure-handling` | Replace bare numeric references in `operations/runner-update-and-rollback.md` with links to the server procedure/failure headings | Preserve the correspondence for failed runner build → server failure handling (2), preparation check (0) → server failure handling (0), and runner build step → server update procedure (4.3); use the verified heading fragments `#43-update-procedure` / `#44-failure-handling`. |

Validation: inspect the exact paragraph/block diff against this map; verify each unique clause remains once and each duplicate is replaced by a local pointer. Check relative links and fragments after editing, run markdownlint on changed Markdown, and report exit codes and any unhandled errors/warnings. No production cheat-sheet or wrapper files are in scope.
